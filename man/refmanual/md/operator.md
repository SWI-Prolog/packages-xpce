# class operator {#class-operator}

Operator (see class parser).  Class operator is part of the
experimental parser package, currently used to parse default
values (see @object_parser).  Use in applications is
discouraged.

